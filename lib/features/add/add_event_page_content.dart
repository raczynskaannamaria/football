import 'package:flutter/material.dart';

class AddEventPageContent extends StatelessWidget {
  const AddEventPageContent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Second'),
    );
  }
}
